// core is plain Java. It declares no libGDX dependency on purpose: that is what
// turns the "core does not depend on libGDX" invariant from a promise into
// something the compiler enforces.
dependencies {
    // Nothing beyond the JDK. Test dependencies come from the root build.
}
